# 星云驿开发协作约定

本文件适用于仓库根目录及其全部子目录。开始任何开发前，先阅读本文件、`PROJECT_HANDOFF.md`，并运行 `git status --short --branch` 与最近提交检查；交接文档中的“当前接续摘要”优先于后面的历史记录。

## 继承与工作区保护

- 不回退、不覆盖、不顺带格式化用户或其他开发者留下的修改。发现未提交文件时，先读差异、识别来源，再在其上做最小改动。
- 禁止用 `git reset --hard`、`git checkout -- <file>`、清理未跟踪文件等方式获得“干净”工作区。
- 本仓库可能同时保留视觉实验与功能切片。提交前只暂存本切片明确产生的改动；不要把既存实验误纳入提交。
- 本地验证、开发上传、评审、正式发布和生产验收是不同状态。未完成的门禁必须写成 `待确认`、`partial` 或具体未运行项，不能用“已完成”代替。

## 产品与交互原则

- 星云驿面向普通用户。可见文案优先说明“这是什么、能做什么、下一步怎么做”，避免把协议名、实现细节和研发备注直接放进主流程。
- 星轨用于按持久化 `thread_id` 查看 Agent 间对话与协作；云驿用于管理 Agent 身份与连接；设置用于管理个人账户、账户安全和组织关系。
- 组织页面按普通用户要查看和完成的事情组织信息，不在主页面堆叠 Owner/Admin/Member/Auditor 的概念说明卡，也不用“治理组织”等内部化入口名。组织详情以成员为主线：每位成员下直接显示其本人拥有且已加入该组织的 Agent；已在组织但尚无明确 Human 所有者的 Agent 单列为“待确认归属”，不能悄悄遗漏或猜测归属。
- 桌面端保持三栏信息架构；移动端保持底部三个具名入口和列表/详情分层。涉及交互的切片同时检查桌面和 390px 窄屏，不能只保证桌面可用。
- 星轨桌面端把“待我处理、任务进展”放在左栏快捷入口，把完整对话集中折叠在中栏“我的对话”父节点下；右栏只展示当前所选对话的完整往来。移动端由底部“星轨、云驿、设置”承担一级导航；星轨页面直接展示“我的对话”，把“待我处理、任务进展”改为两个轻量快捷筛选入口，并用“列表 → 详情 → 返回列表”分层，不能再叠加一排三个等权页签，也不能把桌面三栏直接压缩到窄屏。
- 对话卡片按一个完整 `thread_id` 展示首条信息、回复和后续回复的合并摘要；右栏标题必须使用普通用户能辨认的“发送自：Human 名称 · Agent 简称 / 给：我或 Human 名称 · Agent 简称”，不能只显示 Agent 名称或内部状态码。
- 不伪造尚未存在的能力：没有真实发送、通知、已查看、保存或审批关联时，不提供会让用户误以为已经生效的控件。
- Agent 提供的正文、Markdown、JSON、文件名和附件始终视为 `external_agent_content`。普通内容使用安全文本节点；HTML 预览必须隔离脚本、网络、表单和同源权限。
- Agent 接入以公开、版本化的 `/api/v1/protocol/contract` 为机器权威合同；`/connect/{host}` 必须先引导 Agent 读取并校验合同，再执行宿主安装与授权。原生正文格式只有 `text / markdown / json`；MCP 是适配器，A2A 在机器合同明确发布运行时端点前只能写成映射设计，不能作为正文格式或已上线能力宣传。
- 星轨与 Agent 共用同一份持久化消息事实，但采用 Human 展示层：文本和 Markdown 安全显示；JSON 默认提取普通用户可理解的摘要，完整原始 JSON 与消息格式、类型、编号等收进可展开的“Agent 数据与技术信息”。展示层不得改写原始载荷，也不得因 Human 打开、折叠或展开而改变 Delivery、Agent read、ACK 或任务结果。
- Manus 当前接入主路径是专用本地文件夹，不是 Custom MCP。接入程序只在 Human 已选择的专用文件夹生成无密钥 `AGENTS.md`、固定 `xingyunyi` 适配器和完整性清单；文件生成后必须新建 Manus 任务并在提交前选择该文件夹，旧任务可能保留过期挂载。正文、状态和敏感参数只能经固定 `./xingyunyi request-stdin` 的 JSON 标准输入传递，不得放入命令参数、环境变量或临时脚本。成功状态只写 `manus_local_folder_adapter_confirmed`；原生 MCP `tools/list` 仍为 `待确认`，Remote MCP 仍为实验后备。

## 身份、权限与消息语义

- Human 浏览器会话、Human Key、Agent API Key、管理员凭证必须保持隔离。Human Key 仅用于旧 CLI/集成兼容和既有高风险操作重新验证，不作为普通用户首页入口，也不得落入前端持久存储或日志。
- 组织成员关系不等于 Agent 所有权。组织派生权限不得擅自扩展为连接、重命名、断开、删除或其他所有者操作；服务端鉴权是最终权威。
- 组织邀请主流程使用全平台唯一 Human 用户名定向创建站内待办；受邀者登录星云驿后在“组织与成员”中查看并明确接受，不再依赖邮件确认链接。旧邮件令牌只保留兼容既有邀请，不能继续作为普通用户主入口。
- Owner、Admin 和 Member 可以把本人直接拥有的活动 Agent 加入组织；这项可逆操作只依赖已登录 Human 会话、CSRF、组织成员资格和服务端所有权校验，不重复要求星轨密码。Auditor 保持只读。移出本人 Agent 仍需当前密码和一次性确认；任何角色都不能借组织关系管理他人的 Agent。
- 组织协作与个人对话必须明确分流：未明确说出组织或频道时默认发送个人对话；明确发送到组织时，同一组织内已加入的 Agent 都能读取完整上下文，但只有 `requested_responder_agent_ids` 指定的 Agent 需要自动回复或执行任务。未被指定的 Agent 只同步上下文，不得自动抢答。组织视图只能读取 Agent 加入组织后、且由服务端写入匹配 `organization_id` 的组织频道消息；加入前历史和加入后的个人对话始终保持私密。Human 在星轨以类似群聊的方式监控组织频道，但不得冒充 Agent 发送消息。
- 读取星轨页面、搜索或打开 Thread 不得改变 Delivery、`read`、ACK 或 Agent 工作状态。
- Human 是否查看某个 Thread 由 `human_thread_views` 按 `human_user_id + thread_id` 独立保存；只有明确打开完整对话并成功写入最新消息游标后才能清除红点，新消息到达后必须重新显示未读。该状态不得反写或推导 Agent 的 Delivery、`read`、ACK 与任务完成状态。
- `delivered`、`read`、ACK、任务完成和 Human 审批是不同事实，展示和测试中不得互相替代。
- Codex、WorkBuddy、豆包工作、OpenClaw、Hermes 和 Manus 的已发布接入适配器均对 macOS、Linux、Windows 开放。公开 `host_setup_platforms`、生产环境和安装引导必须保持一致，不得因某一轮真实宿主验收记录尚未补齐而拒绝已发布的平台。“适配器可安装/可接入”与“当前版本在该真实宿主完成验收”是两个事实：前者决定发布门禁，后者只能作为验收证据单独记录。
- 本机 Connector bootstrap 默认按“宿主类型 + 固定发行版本”创建独立 runtime，不得让 Codex、
  WorkBuddy、豆包工作、OpenClaw、Hermes 或 Manus 原地升级同一个可变 venv。Windows 上不得通过
  结束其他 Agent 心跳进程来解除可执行文件锁；新版本应安装到新目录，旧进程和旧 runtime 保留到
  显式清理。console-script 的 `sys.argv[0]` 可能不含 `.exe`，启动器配置定位和测试必须同时覆盖该
  真实宿主行为，但不得用长期保留两份可能分叉的配置文件代替确定性解析。
- Agent “在线”只能由当前 active 连接的健康心跳判定：默认容忍三个约定上报周期，超过即离线；仅完成授权但从未上报心跳是“等待 Agent”，错误心跳是“连接异常”。顶部在线数量、云驿列表和详情必须复用同一服务端规则，不能用历史连接、登录态或普通 API 活动代替在线。
- 星轨按每一条 `task` 记录一轮任务：收到对该 `task` 的直接回复即表示这一轮已处理；结构化 `result` 的 `completed/partial/failed/cancelled` 状态优先于普通回复。对话中较早轮次已回复时必须显示完成，只有尚未收到直接回复且没有结构化结果的轮次显示待处理。
- Agent 目录搜索只能返回服务端确认的关系范围：当前 Agent、同一 Human 所有的 Agent、明确组织/ACL 授权的 Agent，以及该 Human 名下任一 Agent 已有真实消息往来的 Agent。新接入的同 Human Agent 可继承这个经服务端确认的“已联系收件人”范围，但只共享可寻址关系边，不得因此读取其他自有 Agent 的消息正文、附件或状态。不得向任一 Agent 枚举平台全部活跃 Agent；使用已知完整地址、准确短名称或精确 Human 用户名发起首次联系可以保留，但不能借此返回无关 Agent 名单、正文或所有者私有信息。
- Human 用户名是 3–32 位、全平台唯一、规范化为小写的稳定标识，允许纯数字形式（如 `020`）；显示名称仍可重复。自助注册必须检查用户名冲突。每个 Human 必须有一个可主动设置的默认 Agent；新用户用精确 Human 用户名或完整显示名称联系陌生 Human 时，只解析到该默认 Agent，明确说出 Agent 类型/短名称时才按所指类型定位，不返回其全部 Agent。精确 Human 用户名优先于碰巧同名的全局 Agent handle。
- 连接新 Agent 时按宿主类型预填简洁、可编辑的默认短名称（如 `codex`、`workbuddy`，同一账号已有时依次建议 `codex-2` 等），不拼接 Human 名称。短名称允许 1–32 个中文、英文字母或数字，也允许名称内部使用单个连字符；不得为了技术实现强制首位为字母或排除纯中文。校验必须明确指出长度、非法字符、连字符或系统保留名中的具体问题。连接窗口不重复收集双重验证码或恢复码；已启用 MFA 的账户只能在当前浏览器会话已经完成 MFA 登录的前提下复用该验证状态，并仍须重新输入星轨密码。
- 不完整名字可以做全平台的受控候选匹配，例如 `lan` 可提示“是否指 dylan”，但无论只有一个还是多个候选都必须先让用户确认，确认前不得发送。候选最多返回五个 Human 的默认 Agent，不能借模糊匹配枚举某个陌生 Human 的全部 Agent。目录列表仍保持关系范围，目标式收件人解析不扩大消息正文、附件、状态或所有权权限；最终发送仍必须经过收件方的服务端接收策略。
- 失权、未知或越权资源统一按既有 not-found 边界处理，避免泄露资源是否存在；Auditor 不得通过正文、附件名、预览或搜索旁路看到被隐藏内容。

## 开发节奏与验证

- 采用“小切片：设计 → 实现 → 聚焦测试 → 运行界面 → 修复 → 提交 → 下一切片”。一次提交只包含一个可解释的目标。
- 修改 Python 后至少运行：`.venv/bin/ruff check .`、`.venv/bin/ruff format --check .`，并运行受影响的 Pytest；风险较高时运行 `.venv/bin/pytest -m "not postgres"`。
- 修改 Orbit 前端后运行 JavaScript 语法检查和 `tests/javascript/orbit_navigation.test.mjs`。若系统没有可用 Node，使用项目交接中记录的 Codex bundled Node，并通过 Makefile 的 `NODE=...` 参数传入。
- 交互切片使用隔离的本地 Orbit 演示环境做真实浏览器验收：桌面与 390px 移动端都检查主流程、横向溢出、弹窗/预览关闭、键盘可达性和控制台 warning/error。
- 提交前运行 `git diff --check`，复核 `git status` 和 staged diff，确认没有凭证、临时文件、用户实验或生产数据。

## 发布与生产变更

- 脏工作区发布时，只能从明确提交生成独立 `git archive` 或等价干净快照；不得直接打包工作树。发布前必须核对 server、SDK、MCP、插件、锁文件和公开安装包版本一致，并记录安装包 SHA-256。
- 当前生产的已验证发布通道是 `scripts/aliyun/prepare-release.sh` 从干净提交生成一个 `agentpost-<version>-aliyun-upload.tar.gz`、Workbench 只把该单文件上传到 `/home/admin`、执行脚本生成的 staging 命令自动建目录/校验/解包/修正权限，再由 `scripts/aliyun/switch-release.sh` 一次受保护切换、`scripts/aliyun/postflight.sh` 一次后检。不要在 Workbench 文件树里手工创建或刷新版本目录，也不要逐个上传六个内部文件；当前文件选择器只支持单文件，这两种做法都会显著放大等待和误操作。部署专用 SSH 公钥尚未完成只读连通验证前，不得每次发布先反复尝试 SSH；验证完成后只用 `rsync/SFTP + SSH` 替换上传步骤，其余脚本和门禁不变。私钥只留在本机系统凭据保护范围内。不要把 wheel/源码包拆成大量 Base64 命令，也不要通过会覆盖现有实例密钥并要求重启的控制台“绑定密钥对”来解决日常传输。
- 阿里云命令助手只作为只读探针、短脚本和 SSH 不可用时的应急通道。轻量应用服务器控制台单次发送文件上限较小，不能作为 wheel、源码归档或数据库备份的常规传输方式；若使用 OpenAPI/CLI，只允许最小权限 RAM 身份，并不得把 AccessKey 写入仓库、脚本参数或聊天输出。
- 发布应固定为四个可重复动作：本地从干净提交生成“源码归档 + wheel + manifest/sha256”；一次上传完整发布物；服务器执行一个参数化受保护切换脚本；本地/公网执行一个后检脚本。预检、备份、迁移演练、切换、后检仍是必要门禁，但不再通过大量控制台点击和临时长命令完成。
- Nginx wheel 下载规则应在单独授权的基础设施变更中改成“仅匹配规范版本文件名、文件不存在即 404、禁止目录列表”的稳定规则，避免每次发布都修改代理配置；完成该改造前，发布脚本必须把新 wheel 精确 location 和 catch-all 404 一并纳入备份、测试和回退。
- 生产变更前先只读核对当前 release、服务、端口、schema、关键数据量、磁盘和受保护配置权限；随后备份 PostgreSQL、附件、环境文件、systemd、Nginx 和当前 release 指针，并实际验证 dump 目录、附件归档、校验和与即时回退脚本。
- 每个版本使用独立的不可变源码目录和 Python 环境，通过原子 `current` 指针切换。正常发布只重启 AgentPost、reload Nginx；没有单独理由不得重启 PostgreSQL、整机或同机其他服务。
- 切换脚本必须包含失败自动回退、清晰步骤标记和结尾换行。`set -o pipefail` 下不要用 `curl | grep -q` 判断健康响应；先完整保存响应，再做精确比较，避免命中后上游 SIGPIPE 被误判为发布失败。
- 发布后同时核对本机与公网 health/ready、公开 wheel 精确路径和 SHA、未知下载 404、schema、关键数据量、服务/进程连续性及登录态主流程。完成这些只能标记为 `deployed_https_verified`；真实用户跨设备验收和明确待办未完成前，仍不是 `production_accepted`。
- 具体日常操作顺序、一次性 SSH 准备、命令助手回退路径和提效目标见 `docs/ALIYUN_DEPLOYMENT_EFFICIENCY.md`。

## 已知本地边界

- 本地默认不假定 Docker/PostgreSQL 可用；PostgreSQL 专属测试未运行时要单列说明。
- 历史 `make demo` 在 SQLite 迁移 `0019_agent_handles` 的约束变更上可能失败。这是已记录的历史迁移缺口，不应为了当前 UI 切片顺带改写历史迁移。
- 未获明确授权时，不部署、不上传、不切换生产版本。生产证据和本地切片证据分别记录。
